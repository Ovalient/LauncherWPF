<Window x:Class="LauncherWPF.Views.DownloadView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:LauncherWPF.Views"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="DownloadView" Height="400" Width="700" ResizeMode="NoResize" WindowStyle="None" WindowStartupLocation="CenterOwner" AllowsTransparency="True" Background="Transparent"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"
        FontFamily="Arial"
        FontStretch="Condensed">

    <Border Margin="20" >
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                              Direction="0" 
                              BlurRadius="10" 
                              ShadowDepth="0"/>
        </Border.Effect>

        <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect Kind=ExpandIn,Duration=0:0:0.1}">
            <materialDesign:Card>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="7*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*"/>
                        <RowDefinition Height="8*"/>
                        <RowDefinition Height="3*"/>
                    </Grid.RowDefinitions>

                    <!-- AppIcon -->
                    <materialDesign:Card Grid.Column="0" Grid.RowSpan="3">
                        <Rectangle HorizontalAlignment="Center"
                                   Height="120" Width="120" 
                                   RenderTransformOrigin="0.5,0.5" >
                            <Rectangle.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <SkewTransform/>
                                    <RotateTransform Angle="-9.098"/>
                                    <TranslateTransform/>
                                </TransformGroup>
                            </Rectangle.RenderTransform>
                            <Rectangle.Fill>
                                <ImageBrush ImageSource="{Binding Icon}"
                                            Stretch="Fill"/>
                            </Rectangle.Fill>
                        </Rectangle>
                    </materialDesign:Card>

                    <DockPanel Grid.Column="1" 
                               Margin="0,10,0,0">

                        <TextBlock Grid.Row="0" 
                                   Text="{Binding Name}" 
                                   FontSize="22" FontWeight="Bold"
                                   HorizontalAlignment="Left" 
                                   VerticalAlignment="Center" 
                                   Margin="20,0,0,0"/>
                        <TextBlock Grid.Row="0" 
                                   Text=" Installation" 
                                   FontSize="22" FontWeight="Bold"
                                   HorizontalAlignment="Left" 
                                   VerticalAlignment="Center"/>
                        <!-- Close Button -->
                        <Button x:Name="Cancel" 
                                Style="{DynamicResource MaterialDesignFlatButton}" 
                                BorderBrush="{x:Null}" 
                                VerticalAlignment="Top" 
                                HorizontalAlignment="Right"
                                Foreground="#DDFFFFFF" 
                                Padding="0" 
                                Margin="10,10,20,0" 
                                Width="30" Height="30">
                            <materialDesign:PackIcon Kind="Close" 
                                                     VerticalAlignment="Center" 
                                                     HorizontalAlignment="Center"
                                                     Foreground="#FF3E3E3E"
                                                     Width="30" Height="30"/>
                        </Button>
                    </DockPanel>

                    <materialDesign:Card Grid.Row="1" Grid.Column="1"
                                         Margin="20,10">
                        <DockPanel Margin="10">
                            <TextBlock Text="Install Location" 
                                       FontSize="18" FontWeight="Bold"
                                       DockPanel.Dock="Top"/>

                            <DockPanel DockPanel.Dock="Top" 
                                       VerticalAlignment="Top">
                                <!-- InstallPath -->
                                <TextBlock Text="{Binding InstallPath}"
                                           FontSize="15" 
                                           VerticalAlignment="Center"/>
                                <!-- Change InstallPath Button-->
                                <Button x:Name="ChangeButton" 
                                        Style="{DynamicResource MaterialDesignFlatButton}"
                                        Content="Change"
                                        DockPanel.Dock="Right" 
                                        HorizontalAlignment="Right"/>
                            </DockPanel>
                            <Border BorderThickness="1" 
                                    Margin="10" 
                                    BorderBrush="#FF3E3E3E">
                                <TextBlock TextWrapping="Wrap"
                                           FontSize="16"                                          
                                           DockPanel.Dock="Top" 
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Center" 
                                           Margin="0">
                                    Pressing the 'Install' button<LineBreak/>starts the download from the FTP server
                                </TextBlock>
                            </Border>

                        </DockPanel>
                    </materialDesign:Card>

                    <!-- Install Button-->
                    <Button Grid.Row="2" Grid.Column="1"
                            x:Name="Ok"
                            Content="Install"
                            Foreground="White" 
                            FontSize="18" FontWeight="Bold"
                            HorizontalAlignment="Right"  
                            Height="40" Width="100" 
                            Margin="0,0,20,0" />

                </Grid>
            </materialDesign:Card>
        </materialDesign:TransitioningContent>
    </Border>
</Window>
